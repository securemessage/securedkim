# Per-file line ceilings for files over the 400-line limit.
# A ratchet: these may shrink, never grow. Regenerate with
#   zig build lint -- --update
# An empty file below this header means the limit is met everywhere.
420 src/dkim.zig
1237 src/main.zig
644 src/verify.zig
