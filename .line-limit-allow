# 2026-07-29, CORRECTED THEN FIXED: an earlier version of this warning said only
#   `zig build lint -- --update` erased the comments here. That was too narrow. The
#   ORDINARY gate run rewrote the file too, on its self-tightening path, so every
#   note was one shrinking commit away from deletion -- including this one. Observed
#   twice the same day and recovered from git both times.
#
#   Fixed at the source in securemilter-lib/tools/check-line-limit.sh: comment and
#   blank lines above the generated header are now carried across a rewrite, verified
#   idempotent over three consecutive rewrites. Rationale goes ABOVE that header.
#
# 2026-07-29, X-13: the implicit listener now binds loopback instead of 0.0.0.0.
#   Highest stakes of the four daemons: a reachable sign-mode port is an
#   unauthenticated signing oracle for the configured domain.
#
# 2026-07-29, test extraction DONE: main.zig 1305 -> 1140. The seven config,
#   listener and mode tests moved to src/main_test.zig, following the
#   settings_test.zig / dmarc_test.zig precedent. Honest rather than cosmetic:
#   parseDkimConfig, parseMode, DkimConfig and Mode were ALREADY pub, so nothing
#   was exported to satisfy a line count.
#
#   STILL HERE ON PURPOSE: the tests using PRIVATE helpers -- stripAngleBrackets,
#   getSendingDomain, addArHeader. Publishing internals to move a test is the worse
#   trade. Also the `test {}` block, which is what pulls the submodule and
#   main_test.zig tests into the build at all.
#
#   Verified load-bearing, not assumed: commenting out the `_ = @import` reference
#   drops the run from 49 to 42 tests. An unreferenced @import is not analyzed, so
#   its tests are silently absent -- and a test that never runs looks exactly like a
#   test that passes (audit 11.29, msgfile.zig).
#
#   STILL OVER GOAL AT 1140. Tests were never the real cause; main() plus the milter
#   callbacks are. Reaching 400 needs the config parser extracted into its own
#   settings.zig the way securearc already has it, which is an architectural change
#   and not bundled here.
#
# Files currently over the 400-line goal, with the ceiling each
# may not exceed. Ceilings tighten by themselves as files shrink; a
# file that reaches the goal drops off this list entirely.
#
# Adding an entry by hand is how you knowingly accept a long file:
#   zig build lint -- --update
# An empty list below means every file meets the goal.
420 src/dkim.zig
1140 src/main.zig
656 src/verify.zig
