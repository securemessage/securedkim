# 2026-07-29, CORRECTED THEN FIXED: an earlier version of this warning said only
#   `zig build lint -- --update` erased the comments here. That was too narrow. The
#   ORDINARY gate run rewrote the file too, on its self-tightening path, so every
#   note was one shrinking commit away from deletion -- including this one. Observed
#   twice the same day and recovered from git both times.
#
#   Fixed at the source in securemilter-lib/tools/check-line-limit.sh: comment and
#   blank lines above the generated header are now carried across a rewrite, verified
#   idempotent over three consecutive rewrites. Rationale goes ABOVE that header.
#
# 2026-07-29, X-13: the implicit listener now binds loopback instead of 0.0.0.0.
#   Highest stakes of the four daemons: a reachable sign-mode port is an
#   unauthenticated signing oracle for the configured domain.
#
# 2026-07-29, REVERTED AND THE METRIC FIXED INSTEAD. Earlier the same day I moved
#   seven tests out of main.zig into a new src/main_test.zig and recorded 1305 -> 1140
#   as a reduction. THAT WAS GAMING THE METRIC, and the user caught it.
#
#   The checker excluded *_test.zig FILES but counted test BLOCKS inside the files it
#   did check, so the identical test counted here and counted for nothing there. The
#   only thing relocation changed was the number. Production lines were 1107 before
#   and 1107 after -- measured, not estimated.
#
#   Worse, I had already noticed it did not work: the note this replaces said "STILL
#   OVER GOAL AT 1140, tests were never the real cause" and I shipped the ceiling
#   reduction anyway. Recording that a change achieved nothing is not a substitute for
#   not making it.
#
#   Zig's convention is a test beside the thing it tests, so main_test.zig is deleted
#   and the tests are back where they belong.
#
#   THE COUNTER NOW MEASURES PRODUCTION LINES ONLY, in any file (see
#   securemilter-lib/tools/check-line-limit.sh). Tests never count, wherever they
#   live, which removes the incentive entirely.
#
#   1140 -> 1154 is NOT growth. It is a change of basis: the tests came back, and the
#   two test HELPERS (parseForTest, freeTestConfig) plus their comments sit outside
#   test blocks and so are counted. ~47 lines of honest overhead for co-locating
#   tests. The old 1140 measured a different thing.
#
#   THE REAL SIZE IS 1107 PRODUCTION LINES and this is the largest file in the suite.
#   main() plus the milter callbacks are the bulk. Reaching 400 needs the config
#   parser extracted into its own settings.zig the way securearc has it -- an
#   architectural change, and the only one that would actually reduce anything.
#
# 2026-07-30, X-16(b): main.zig +6, RAISED DELIBERATELY. Same change in all four daemons.
#
#   Every fatal startup failure used to be invisible. `daemonize` points stderr at
#   /dev/null, so the error Zig's start code prints for an error returned from `main`
#   goes to a descriptor nobody reads, and syslog -- the only channel still open after
#   the fork -- never received it. Measured: a daemon told to bind an unusable address
#   exited 0, left nothing listening, and logged "starting" as its last word.
#
#   The fix is the SHAPE of `main`, not a line inside it. The body moved to `runDaemon`
#   and `main` became one line:
#
#       runDaemon() catch |e| return bootstrap_mod.fatal(e);
#
#   so every error converges on one report -- including from a `try` added later.
#   Per-site catch blocks were the alternative, and are how X-7 came to be fixed in one
#   of four copies.
#
#   +6 is the entire cost: three lines of `main`, one blank, and a two-line comment
#   pointing at `bootstrap.fatal` where the reasoning is written once. It was +10 until
#   the gate objected to the same six-line rationale appearing in four files, which was
#   a fair objection and is why it now lives in the library.
#
#   NOT PAID DOWN HERE ON PURPOSE. These four main.zig files are already the roadmap's
#   next extraction target; +6 against a planned several-hundred-line reduction is
#   noise, and this ceiling should be overtaken by that work rather than by a separate
#   tidy-up.

# 2026-07-30, X-16(a): main.zig +3, RAISED DELIBERATELY. Same change in all four daemons.
#
#   The readiness handshake. `daemonize` now returns the write end of a pipe and the
#   ORIGINAL process blocks on the read end instead of exiting 0 immediately, so
#   `service start` finally reports what happened. Measured before: an unbindable
#   listener gave parent exit 0, nothing listening, no process, and rc.d announcing a
#   started service. After: exit 1.
#
#   The daemon has to say when it is actually serving, and only the daemon knows --
#   everything that makes it useful (binding, the worker pool) happens AFTER
#   bootstrap.run returns. So one line lands here, after the pool spawns:
#
#       boot.notifyReady();
#
#   +3 is comment, call, blank. The reasoning is in Bootstrap.notifyReady's doc
#   comment, not repeated here -- the first attempt carried a four-line rationale in
#   all four daemons and the gate objected, correctly, for the second time in one day.
#
#   NOT PAID DOWN HERE ON PURPOSE, same as the X-16(b) note above: these files are the
#   roadmap's settings.zig extraction target and should be cut there, not nibbled here.

# 2026-07-30, D-24: main.zig 1065 -> 1163, RAISED AND BEING PAID BACK IMMEDIATELY.
#
#   Unlike the X-16 notes above, +98 is not noise and is not being deferred to the
#   settings.zig extraction. It is paid down in the very next commit.
#
#   +98, not the +66 this note first claimed, and the difference is worth stating
#   rather than quietly editing the number: +66 was the validation, then +15 came
#   from freeing the three parseDkimConfig slices on the way out (a startup failure
#   used to print its reason followed by three GPA "leaked" lines, which is how a
#   reader learns to ignore leak output) and +17 from the D-24 test helpers, which
#   sit outside test blocks and so count -- the same honest overhead the 2026-07-29
#   note above measured at ~47 lines.
#
#   What was added: table-based signing previously resolved d=/s= from the KeyTable
#   while the key came from the single-domain KeyFile, with nothing making the two
#   agree. Measured in the lab: a host with both configured emitted d=b.example
#   signed with a.example's key -- caught by SIGNATURE LENGTH (344 base64 chars, a
#   2048-bit key, where b.example's 3072-bit key would give 512), because the daemon
#   said nothing that would have revealed it. A table-ONLY config, which is the
#   stanza the shipped sample advertises, signed nothing at all and logged nothing.
#
#   The +66 is the config-time validation that makes those states unreachable:
#   loading every KeyTable row's key against the RFC 8301 floor, and refusing a
#   SigningTable without a KeyTable, a KeyTable without a SigningTable, or a signing
#   entry the KeyTable does not define. It is bulk because each refusal names what is
#   wrong -- which is the entire point, the old behaviour being silence.
#
#   OBLIGATION: the signing-asset lifecycle (MAX_TABLE_BYTES, loadSigningTable,
#   loadKeyTable, buildSigningAssets, freeSigningAssets and this validation) is one
#   cohesive unit and moves to src/signing.zig next, taking main.zig back UNDER 1065.
#   That is the number to hit; if the next commit does not hit it, this note is the
#   evidence the debt was taken and not repaid.
#
#   PAID, same day, next commit: 1163 -> 964, against the 1065 promised. src/signing.zig
#   is 355 lines and so does not join this list at all. `resolve` and the three
#   shorthand globals went with the loaders rather than staying behind, because the
#   module is only cohesive if it holds all three of what the assets ARE, how they are
#   validated, and which one applies to a message -- D-24 WAS the third drifting from
#   the other two. The 1163 ceiling above is left in place as the record of what was
#   borrowed; the generated list below is the truth about what is owed now.

# 2026-07-31, D-23: main.zig 1002 -> 1008, +6.
#
#   This daemon now negotiates SMFIP_HDR_LEADSPC so `c=simple` can hash a header
#   field as it actually arrived rather than as `name + ": " + value`. The lines
#   are the `protocol_flags` entry and three short comments at the sites that
#   rebuild a field -- the verify header list, the sign header list, and the
#   DKIM-Signature self-reference. The rule itself lives once, in
#   `securemilter-lib` `header.zig`; these only point at it.
#
#   No code was added: the three sites got shorter, since `hdr.render(alloc)`
#   replaced an allocPrint with a format string.
#
# 2026-07-31, THE settings.zig EXTRACTION: main.zig 1008 -> 777, and the two
#   "NOT PAID DOWN HERE ON PURPOSE" notes above (X-16(a) and X-16(b)) are now paid.
#   Both deferred +6 and +3 explicitly to this work rather than nibbling at the
#   file; -231 covers them many times over, and the ceiling tightened by itself.
#
#   src/settings.zig (286 production lines, under the goal so it joins no list)
#   holds Mode, DkimConfig and parseDkimConfig with the config tests beside them.
#   Every name is re-exported from main.zig at its old spelling, so not one call
#   site moved -- the same handling securearc gave its own split, and the reason
#   this shows up as a pure deletion in main.zig rather than a rename everywhere.
#
#   This file was the largest in the suite at 1008. It is not any more:
#   securespf/src/evaluate.zig at 861 now leads.
#
# 2026-07-31, D-6/D-14/D-17: main.zig 777 -> 792 (+15), verify.zig 443 -> 454 (+11).
#
#   verify.zig: `parseSignature`'s failures were all reported as "malformed signature".
#   They now map to their own reasons, because a duplicate tag (D-6) and a `c=` we
#   cannot perform (D-14) have different causes and different fixes. Ten lines of
#   switch and four of comment.
#
#   main.zig: the reason `verify.zig` computes for every non-pass verdict is now
#   logged. It was computed and discarded for everything except the weak-key case, so
#   `dkim=fail` reached the postmaster with the body-hash/signature distinction already
#   thrown away -- the fact whose absence made diagnosing D-15 and D-16 require
#   reimplementing the hash in Python (D-17).
#
#   Both comments were cut back before taking the raise; the first drafts were roughly
#   double. main.zig is also 216 lines below where it stood this morning, since the
#   settings.zig extraction took it from 1008 to 777 -- this gives back 15 of that.
#
#   NO OBLIGATION TO UNDO. Neither change created a seam; the extraction already took
#   the one main.zig had.
#
# 2026-08-04, STAGE 4.1: main.zig 831 -> 418, and a new flow.zig at 501.
#
#   The message flow moved to `flow.zig` -- onBody, the end-of-message pipeline,
#   doVerify, doSign, and the A-R/event/utility helpers that only they call.
#   This is the decomposition securearc has had since R1 and securespf took at
#   stage 4.3, and it is the second half of 4.1: the `settings.zig` half landed
#   as SS11.48, which is why main.zig was 831 here and not the 1155 the plan
#   estimated from.
#
#   NOT A PURE MOVE, AND SAYING SO. Repo functions 104 -> 106. The two are
#   `msgCtx`, which gathers the globals the flow reads at the one point per
#   message where they are known to agree, and `onEom`, now a wrapper in
#   main.zig over `flow.doEom` -- the callback signature is fixed by
#   worker.Callbacks and cannot carry a context. `onBody` needed none of them
#   and is registered as `flow.onBody` directly. Unit tests 70 -> 71; the +1 is
#   a test the extraction earned, see below.
#
#   flow.zig's MsgCtx DELIBERATELY DIVERGES from securearc's, which holds
#   `*Resolver` and `*Publisher` directly. This one holds accessors. securearc
#   validates every message so its resolver is always used; this daemon has a
#   whole `sign_only` mode that reaches no DNS at all, and doSign returns early
#   for a message with no envelope sender or no signing-table entry. Eager
#   fields would build a DNS resolver with its TTL cache and open a ZMQ socket
#   on a worker thread that is only ever signing. Same reasoning, and the same
#   choice, as securespf/src/flow.zig.
#
#   501 IS RECORDED RATHER THAN SPLIT FURTHER, and 418 likewise. doVerify (140)
#   and doSign (116) are the bulk of flow.zig and each runs one RFC 6376
#   procedure end to end -- SS6.1 verification and SS3.7 signing. Cutting either in
#   half would put a rule and its error handling in different files, which is
#   the reason the plan gives for leaving securearc/src/chain.zig alone. main.zig
#   at 418 is 18 over the goal and is NOT being trimmed to reach it: what is
#   left is the globals with their audit rationale, the bootstrap, and the
#   reload, and deleting explanation to cross a threshold is what SS11.31 is
#   about. Both should fall further at stage 5, which dedupes the tools.
#
#   One test was written because the move made it possible: `modeFor` took a
#   module global, so nothing could set it from a test, and its out-of-range
#   fallback is exactly what made A-2 a bypass. It takes the slice now and the
#   fallback is asserted -- including that an empty mode list yields
#   verify_only and never a signing mode.
#
#   `auth_results`, `auth_stamp`, `escape`, `codec` and `responses` were used
#   only by the moved functions and are removed from main.zig. `listener_mod`
#   went with them but was ALREADY DEAD at HEAD -- confirmed with `git show`,
#   not assumed, and not claimed as a result of this pass.
#
#   verify.zig IS LEFT BREACHING ON PURPOSE. It is 637 against a ceiling of
#   454, it has been over since 2026-07-29 (`79238740`, the RFC 8463 suite that
#   found D-18/D-19), and this file does not accept it. Running
#   `lint -- --update` here would have raised it to 637 silently, which is the
#   accretion the gate exists to catch. It is the next item, and until it is
#   addressed this repo's gate stays red -- which is the correct state.

# WRITE THE REASON FOR A CEILING ABOVE THIS LINE.
#
# From here down is regenerated whenever a ceiling moves. A comment
# above that line is preserved; one below it is reported and then lost.
#
# Files currently over the 400-line goal, with the ceiling each
# may not exceed. Ceilings tighten by themselves as files shrink; a
# file that reaches the goal drops off this list entirely.
#
# Adding an entry by hand is how you knowingly accept a long file:
#   zig build lint -- --update
# An empty list below means every file meets the goal.
418 src/main.zig
501 src/flow.zig
454 src/verify.zig
